<h1>My Tasks</h1>
<% @tasks.each do |task| %>
  <h3>[<%= task.completed ? "x" : "   " %>] <%= link_to task.title, task_path(task) %></h3>
<% end %>

<%= link_to "+ creat a new tack", new_tasks_path %>
